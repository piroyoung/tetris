interface:
  display_name: "React SPA Architecture"
  short_description: "Own code architecture, UI guardrails, and verification in React Router + Prisma SPA work"
  default_prompt: "Use $enforce-react-spa-architecture to bootstrap or implement this React Router and Prisma v7 SPA with strict layer boundaries, Fluent UI React v9 for new UI work, commit hygiene, and pre-push verification."
