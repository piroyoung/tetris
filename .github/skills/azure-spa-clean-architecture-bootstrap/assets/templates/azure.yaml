name: __APP_NAME__

metadata:
  template: azure-spa-clean-architecture-bootstrap

services:
  __SERVICE_NAME__:
    project: .
    host: containerapp
    language: js
    docker:
      path: ./Dockerfile

hooks:
  postprovision:
    shell: sh
    run: ./scripts/azure/postprovision.sh
    continueOnError: false
